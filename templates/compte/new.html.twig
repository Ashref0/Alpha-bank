{% extends 'basefront.html.twig' %}

{% block title %}New Compte{% endblock %}

{% block body %}

<div class="container mt-5">
    <h1>Create new Compte</h1>

    {{ include('compte/_form.html.twig') }}

    <a class="btn btn-dark" href="{{ path('app_compte_index') }}">back to list</a>
</div>
{% endblock %}
