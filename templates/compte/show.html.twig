{% extends 'basefront.html.twig' %}


{% block title %}Compte{% endblock %}

{% block body %}
    <h1>Compte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compte.id }}</td>
            </tr>
            <tr>
                <th>Num_compte</th>
                <td>{{ compte.numCompte }}</td>
            </tr>

            <tr>
                <th>Type_compte</th>
                <td>{{ compte.TypeCompte }}</td>
            </tr>
           
        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('app_compte_index') }}">back to list</a>

    <a class="btn btn-primary" href="{{ path('app_compte_edit', {'id': compte.id}) }}">edit</a>

    {{ include('compte/_delete_form.html.twig') }}
{% endblock %}
