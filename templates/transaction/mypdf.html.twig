<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Transaction Details</title>
    <style>
        body {
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            padding: 20px;
            color: #333;
            background-color: #f8f9fa;
        }
        .container {
            background-color: #fff;
            border: 1px solid #dee2e6;
            border-radius: 0.25rem;
            padding: 20px;
            margin-top: 20px;
        }
        .header {
            border-bottom: 2px solid #eee;
            padding-bottom: 10px;
            margin-bottom: 10px;
        }
        .attribute {
            font-weight: bold;
            color: #555;
        }
        .value {
            margin-bottom: 10px;
            color: #000;
        }
        .footer {
            border-top: 2px solid #eee;
            padding-top: 10px;
            margin-top: 10px;
            text-align: center;
            font-size: 0.8em;
            color: #777;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>Transaction Details</h2>
        </div>
        <p><span class="attribute">Transaction Date:</span> <span class="value">{{ data.dateTransaction|date('Y-m-d') }}</span></p>
        <p><span class="attribute">Recipient Name:</span> <span class="value">{{ data.nomDestinataire }}</span></p>
        <p><span class="attribute">Recipient Surname:</span> <span class="value">{{ data.prenomDestinataire }}</span></p>
        <p><span class="attribute">Recipient IBAN:</span> <span class="value">{{ data.ibanDestinataire }}</span></p>
        <p><span class="attribute">Amount:</span> <span class="value">{{ data.montant }}</span></p>
        <p><span class="attribute">Sender IBAN:</span> <span class="value">{{ data.iban }}</span></p>
        <p><span class="attribute">Client Name:</span> <span class="value">{{ data.client.nom }}</span></p>
        <div class="footer">
            Generated by Alpha bank
        </div>
    </div>
</body>
</html>
